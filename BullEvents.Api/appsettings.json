{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Seed": {
    // Demonstration material — extra tenants, sample leads, the generated
    // contact base, the four showcase projects and their traffic. Off, because
    // this install carries the company's own records; switch it on only on a
    // throwaway database where the dense list views are what is being tested.
    "Demo": false
  }
}
